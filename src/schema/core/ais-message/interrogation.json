{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://poseidat.org/schema/core/ais-message/interrogation.json",
  "title": "Interrogation send by the AIS",
  "description": "Request for a specific message types from multiple stations or ships",
  "properties": {
    "ais_message_type": {
      "description": "The message type of the given AIS measurement",
      "type": "string",
      "enum": [
        "INTERROGATION"
      ],
      "default": "INTERROGATION"
    },
    "requested_message_type": {
      "description": "the requested message type used in message type 15 (interrogation); Can request up to two message types from two stations",
      "type":"array",
      "items": {
        "$ref": "https://poseidat.org/schema/enum/ais-message-type.json"
      },
      "maxItems": 2
    },
    "destination_ids": {
      "description": "The MMSI numbers where the ship is sending data to.",
      "type": "array",
      "items": {
        "type": "integer"
      },
      "maxItems": 2
    }

  },
  "required": ["ais_message_type", "requested_message_type", "destination_ids"],
  "allOf": [
    { "$ref": "https://poseidat.org/schema/core/ais-message/base-ais-message.json" }
  ],
  "type": "object",
  "additionalProperties": false
}
