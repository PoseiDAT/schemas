{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://poseidat.org/schema/entry/equipment-inventory/device.json",
  "title": "Device",
  "description": "A device which is a part of a piece of equipment installed on a vessel",
  "properties": {
    "device_id": {
      "type": "string",
      "format": "uuid",
      "description": "The unique identifier for the device (UUID v4)"
    }
  },
  "required": [
    "device_id"
  ],
  "type": "object",
  "additionalProperties": false
}
